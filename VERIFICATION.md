# 交付前验证记录

这份文件如实记录**已经验证过什么**、**用什么方法验证**、以及**还没有验证什么**。
请先读「你需要做的第一件事」。

---

## ⚠️ 你需要做的第一件事

构建这套代码的沙箱环境**无法访问 npm registry**（所有请求返回 403），
因此我没有办法在交付前运行 `npm install`，也就无法运行真正的
`tsc --noEmit` / `next lint` / `next build`。

所以拿到项目后，请先执行：

```bash
npm install
npm run typecheck     # TypeScript
npm run lint          # ESLint
npm run build         # 生产构建
```

**预期**：可能会有少量类型错误需要修。我用静态分析尽力压低了这个概率（见下），
但没有编译器兜底，不敢向你保证一次全绿。如果报错，绝大多数会是
「某个 Prisma 查询的返回类型与页面里的取值不完全对齐」这一类，改动很小。

数据库结构本身我用真实 PostgreSQL 16 验证过（见第 2 节），这一层可以放心。

---

## 0. 怎么自己复现这些结论

本文里的每个结论都对应 `tools/verify/` 下一个可以再跑一次的脚本，用法见
`tools/verify/README.md`。最快的一条：

```bash
node tools/verify/static-check.mjs      # 不需要装依赖
python3 tools/verify/validate-schema.py # 不需要装依赖
```

---

## 1. 静态分析（已全部通过）

在 143 个 TS/TSX 文件上跑了自己写的检查器（最后一次运行：**错误 0 条，警告 0 条**）：

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 未使用的 import | ✅ 0 处 | `@typescript-eslint/no-unused-vars` 配的是 `error`，这类问题会直接让构建失败 |
| `@/` 路径能否解析到真实文件 | ✅ 全部可解析 | 含 `index.ts` 目录解析 |
| 导入的具名导出是否真实存在 | ✅ 全部存在 | 跟随 `export * from` 递归展开 |
| `prisma.<model>` 名称是否有效 | ✅ 全部有效 | 对照 schema 的 37 个模型 |
| Prisma 查询里的键名 | ✅ 无可疑键 | 把每个 `prisma.x.y({...})` 的参数键与 schema 字段 + Prisma 操作符集合比对，抓拼写错误 |
| `Record<Enum, …>` 映射表是否穷尽 | ✅ 44 组全部穷尽 | 这是最容易出错的地方 —— 少一个枚举成员就是类型错误 |
| 从 `@prisma/client` 导入的 37 个类型 | ✅ 全部存在于 schema | |
| 客户端组件误引 `server-only` 模块 | ✅ 0 处 | |
| 客户端组件直接访问 `prisma` | ✅ 0 处 | |
| 服务端组件里使用 React hooks | ✅ 0 处 | |
| Tailwind 颜色类是否都在调色板内 | ✅ 全部命中 | 逐个比对 `brand/warm/danger/info/ink/cream` 的实际档位 |
| 非标准间距类（如 `h-13`） | ✅ 已清理 | 发现并修正了 1 处 |
| 括号 / JSX 花括号是否平衡 | ✅ 全部平衡 | 抓结构性断裂（曾抓出 1 处未闭合的条件渲染） |
| 每个后台页面都有守卫 | ✅ 全部有 | 业务页 `guardPage('<capability>')`，个人页 `guardPortal('admin')` |
| `guardPage` / `can()` 引用的能力是否存在 | ✅ 全部存在 | 对照 `capabilities.ts` 的 50 项能力 |
| 侧边栏导航绑定的能力是否存在 | ✅ 全部存在 | 导航按能力裁剪，不存在的能力会让菜单永久隐藏 |
| Server Action 是否都有 `'use server'` + zod | ✅ 全部有 | 每个入口都做参数校验 |
| Server Action 是否直接写库 | ✅ 0 处 | 业务规则全部在 `services/`（本轮把 2 处直写迁回服务层） |
| Prisma `select` / `include` 里的非字面量布尔 | ✅ 0 处 | 这类写法会破坏类型推导，是构建失败的常见原因 |
| 界面文案是否出现禁用称呼 | ✅ 0 处 | 「服务阿姨 / 陪诊阿姨 / 护工 / 陪护 / 保姆 / 护理员 / 工作人员」一律禁止；对长辈的敬称（如「王阿姨表示…」）会单独提示人工确认，本轮据此改掉了 1 句有歧义的示例文案 |
| 硬编码密钥 / 密码 | ✅ 0 处 | 仅 `prisma/seed.ts` 与登录页 Demo 提示例外，两者都只在 `NEXT_PUBLIC_DEMO_MODE=true` 下出现 |

---

## 2. 数据模型（用真实 PostgreSQL 16 验证）

沙箱里起了一个真实的 PostgreSQL 16 实例，把 `schema.prisma` 机械翻译成 DDL 后实际执行。

### 2.1 结构校验（自写的 Prisma schema 检查器）

| 检查项 | 结果 |
| --- | --- |
| 字段类型是否已知（标量 / 枚举 / 模型） | ✅ |
| 每个模型都有 `@id` 或 `@@id` | ✅ |
| `@@unique` / `@@index` / `@@id` 引用的字段存在 | ✅ |
| `@relation` 的 `fields` 引用本模型真实字段 | ✅ |
| `@relation` 的 `references` 引用目标模型真实字段 | ✅ |
| `fields` 与 `references` 数量一致、类型一致 | ✅ |
| 被引用字段是 `@id` 或 `@unique` | ✅ |
| 每个关系都有配对的反向关系字段 | ✅ 修正了 1 处（`City` 缺少 `households`） |
| 一对一关系的外键带 `@unique` | ✅ |
| 关系可选性与外键可选性一致 | ✅ |
| 列表字段没有同时标可选 | ✅ |

### 2.2 DDL 实际执行结果

```
tables:               37
enums:                40
foreign keys:         64
unique constraints:   32
indexes（含主键/唯一）: 108
```

DDL 以 `ON_ERROR_STOP=1` 执行，全部成功、退出码 0 —— **没有外键环、没有类型冲突、没有非法默认值**。

### 2.3 端到端数据链路（真实 INSERT）

按主流程顺序把整条链插进真实数据库，一次事务全部成功：

```
城市 → 区域 → 服务方案 → 服务目录(4) → 通知模板(2) → 用户(6) → 专员档案(2)
→ 家庭(2) → 长辈(2) → 敏感信息 → 家庭成员(3) → 授权(9，含 1 条已撤回)
→ 订阅 → 订单(2，含已开票 / 待开票) → 服务任务(3) → 原始服务记录(2)
→ 健康数值 → 安心报告(2) → 报告↔原始记录引用 → 异常事件
→ 家庭时间线(3，含 1 条内部记录) → 站内通知 → 审计日志
```

这证明了外键依赖顺序是可行的 —— 也就是 `prisma/seed.ts` 的写入顺序不会撞外键。

### 2.4 隔离与合规规则（用纯 SQL 复现服务端判定，22 项全部符合预期）

| # | 检查 | 期望 | 实际 |
| --- | --- | --- | --- |
| 1 | 王女士（付款人）可见的健康敏感信息 | 0 | **0** ✅ |
| 2 | 王女士可见的安心报告（本家庭 + 有授权） | 1 | **1** ✅ |
| 3 | 王女士可见的刘家报告（跨家庭隔离） | 0 | **0** ✅ |
| 4 | 王先生可见的安心报告（他没有报告授权） | 0 | **0** ✅ |
| 5 | 王先生可见的生活记录（授权已撤回） | 0 | **0** ✅ |
| 6 | 撤回后历史授权记录仍保留 | 1 | **1** ✅ |
| 7 | 王女士可见的对外时间线（内部记录不出现） | 2 | **2** ✅ |
| 8 | 李晨可见的家庭数（只应是自己负责的） | 1 | **1** ✅ |
| 9 | 一线专员可访问的健康敏感信息 | 0 | **0** ✅ |
| 10 | 周敏可见的王家任务（专员之间隔离） | 0 | **0** ✅ |
| 11 | 已发布报告的原始记录溯源条数 | 1 | **1** ✅ |
| 12 | 健康数值缺少来源标注的条数 | 0 | **0** ✅ |
| 13 | 标记「超出家庭设定范围」的数值 | 1 | **1** ✅ |
| 14 | 查看敏感信息是否留下操作日志 | 1 | **1** ✅ |
| 15 | 家庭可自助申请的服务类型数 | 2 | **2** ✅ |
| 16 | 开放自助申请但没有加购价格的类型 | 0 | **0** ✅ |
| 17 | 启用了未实现通知渠道的模板 | 0 | **0** ✅ |
| 18 | 报告类通知模板是否绑定所需授权 | 1 | **1** ✅ |
| 19 | 待开票订单数（发票为人工流程，状态可追踪） | 1 | **1** ✅ |
| 20 | 已开票但没有回填发票号的订单 | 0 | **0** ✅ |
| 21 | 服务照片只存对象存储 key（不得是公开 URL） | 1 | **1** ✅ |
| 22 | 未经审核就发布的报告 | 0 | **0** ✅ |

第 1 行是「**付款权 ≠ 数据查看权**」在数据库层面的直接证据：
王女士是付款人、主要联络人、紧急联络人，仍然看不到慢性情况与用药。

第 5、6 行一起证明了撤回语义：撤回**立即失效**，但历史记录**永不删除**。

第 9 行证明一线安心专员在任何路径下都拿不到健康敏感信息 ——
敏感数据在无权限时甚至不会被查询出来（`getElder` 拆成了两次查询）。

---

## 3. 还没有验证的部分（诚实清单）

| 项目 | 原因 | 你可以怎么补 |
| --- | --- | --- |
| `tsc --noEmit` | 沙箱装不了 `typescript` 与各类 `@types` | `npm run typecheck` |
| `next lint` | 同上 | `npm run lint` |
| `next build` | 同上 | `npm run build` |
| `prisma generate` / `prisma validate` | 沙箱装不了 prisma CLI | `npm install` 会自动跑 `prisma generate` |
| 浏览器里的真实点击流 | 没有可运行的服务 | 按 README 第六节的 20 步演示路线走一遍 |
| 三端的视觉呈现 | 同上 | 同上 |
| Server Action 的运行时行为 | 同上 | 同上，或直接看 `/api/dev/selfcheck` |
| npm 依赖版本能否解析 | registry 不可达 | `npm install` 时若某个版本不存在，把该条改成 `^` 范围即可 |

### 补偿措施：内置自检端点

因为无法在交付前跑通浏览器流程，我把验证做进了代码里。
配置 `DEV_CHECK_TOKEN` 后访问：

```
GET /api/dev/selfcheck?token=<你的令牌>
```

它会在服务端跑 **50+ 项断言**，覆盖：

- E2E 主流程 13 步（建家庭 → 授权 → 派单 → 记录 → 报告 → 家庭端可见）
- 家庭之间、专员之间的数据隔离
- 角色能力隔离（运营 / 财务 / 人事 / 一线专员）
- 授权模型（含「付款人身份不带来查看权限」）
- 报告可追溯性（统计与原始记录一致、不含记录里没有的事项）
- 派单闸门、学历展示纪律、审计日志、Demo 标记、事件时间线
- 服务目录：开放家庭自助申请的类型必须有价格；**真实尝试申请一个未开放的类型并断言被拒绝**
- 通知模板：占位符必须在白名单内、不得启用尚未实现的渠道、涉及长辈数据的模板必须绑定授权
- 发票：标记已开票的订单必须已回填发票号
- 运营指标：续费率 / 家庭满意度 / 利用率可计算，且样本为 0 时返回 `null` 而不是 `0`

它用的是**真实的服务层函数与权限判定代码**，不是另写一套逻辑。
所以 `npm run db:seed` 之后访问一次这个端点，等于把第 4 节要求的隔离测试全跑了。

生产环境请把 `DEV_CHECK_TOKEN` 留空 —— 端点会直接返回 404。

---

## 4. 环境里发生过的事（供参考）

- **npm registry 403**：`registry.npmjs.org`、`registry.npmmirror.com`、`registry.yarnpkg.com` 全部被拦，
  内网 Artifactory 需要凭证。所有依赖相关验证因此无法进行。
- **GitHub 只允许预配置仓库**：无法新建仓库，因此走不了「推 GitHub → Vercel 从 git 构建」这条路。
- **Vercel 令牌没有建项目权限**：`403 You don't have permission to create a project`，
  因此也走不了「直接上传文件到 Vercel 构建」这条路。
- **Supabase**：已在你的组织（新馬台香）下创建了一个空项目
  `jiating-anxin-guanjia`（`zkomnapkkpbqlnqswxgs`，ap-southeast-1，免费额度 $0/月）。
  里面**没有写入任何数据**。如果你不需要，可以在 Supabase 控制台直接删掉。
  如果要用，在项目设置里拿到数据库密码填进 `.env` 的 `DATABASE_URL` / `DIRECT_URL` 即可。
